/** @type {import('next').NextConfig} */
const nextConfig = {
  // Babel config (babel.config.js) will automatically disable SWC
  // and apply the AI source plugin via next/babel preset
}

module.exports = nextConfig
